
{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "count": {
      "type": "integer",
      "description": "The number of items to return per page."
    },
    "page": {
      "type": "integer",
      "description": "The page number to return."
    },
    "status": {
      "type": "string",
      "description": "The status of the forms to return."
    },
    "order_by": {
      "type": "string",
      "description": "The field to order the results by."
    },
    "asc": {
      "type": "boolean",
      "description": "Whether to order the results in ascending or descending order."
    }
  },
  "required": []
}


This is a JSON schema describing the query parameters for the `GET /public/v1/forms` endpoint. It specifies that the request can include the following parameters:

- `count` (integer): The number of items to return per page.
- `page` (integer): The page number to return.
- `status` (string): The status of the forms to return.
- `order_by` (string): The field to order the results by.
- `asc` (boolean): Whether to order the results in ascending or descending order.

None of the parameters are required.