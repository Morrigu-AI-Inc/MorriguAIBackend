
{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "description": "The type of objects to filter by, either 'lead' or 'contact'.",
      "enum": ["lead", "contact"]
    },
    "object_type": {
      "type": "string",
      "description": "The type of objects to return, either 'lead' or 'contact'.",
      "enum": ["lead", "contact"]
    },
    "name": {
      "type": "string",
      "description": "The name of the smart view."
    },
    "query": {
      "type": "object",
      "description": "The query parameters for filtering the results."
    },
    "id": {
      "type": "integer",
      "description": "The ID of the smart view."
    }
  },
  "required": ["name", "query"]
}
