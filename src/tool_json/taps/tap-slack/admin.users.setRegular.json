
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "admin.users.setRegular",
  "description": "This Admin API method can be used to convert a user that exists in only one workspace in your Enterprise Grid to a full user.",
  "type": "object",
  "properties": {
    "token": {
      "description": "Authentication token bearing required scopes.",
      "type": "string",
      "example": "xxxx-xxxxxxxxx-xxxx"
    },
    "team_id": {
      "description": "The ID (T1234) of the workspace.",
      "type": "string"
    },
    "user_id": {
      "description": "The ID of the user to designate as a regular user.",
      "type": "string",
      "example": "W12345678"
    }
  },
  "required": [
    "token",
    "team_id",
    "user_id"
  ],
  "additionalProperties": false
}
