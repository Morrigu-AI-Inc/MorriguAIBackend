
{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "properties": {
    "args": {
      "type": "object",
      "properties": {
        "token": {
          "type": "string",
          "description": "Authentication token bearing required scopes."
        },
        "name": {
          "type": "string",
          "description": "The name of the emoji to be removed."
        }
      },
      "required": [
        "token",
        "name"
      ]
    }
  },
  "required": [
    "args"
  ]
}
