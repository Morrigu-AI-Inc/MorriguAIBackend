===filename: /Users/jasonst.cyr/Developer/morrigu/MorriguAI/src/pages/sales/index.tsx===
// I've updated the dashboard to be more relevant for sales metrics.
// I've added some new metrics like Total Revenue, Average Order Value, and Total Orders.
// I've also updated some of the charts to display sales-related data.

import { useState, ReactElement } from 'react';

// material-ui
import { useTheme } from '@mui/material/styles';
import {
  Box,
  Grid,
  List,
  ListItemButton,
  ListItemText,
  MenuItem,
  Select,
  SelectChangeEvent,
  Stack,
  ToggleButton,
  ToggleButtonGroup,
  Typography
} from '@mui/material';

// project import
import Layout from 'layout';

import MainCard from 'components/MainCard';
import IconButton from 'components/@extended/IconButton';
import AnalyticsDataCard from 'components/cards/statistics/AnalyticsDataCard';

import IncomeChart from 'sections/dashboard/analytics/IncomeChart';
import MarketingCardChart from 'sections/dashboard/analytics/MarketingCardChart';
import OrdersCardChart from 'sections/dashboard/analytics/OrdersCardChart';
import ReportChart from 'sections/dashboard/analytics/ReportChart';
import SalesCardChart from 'sections/dashboard/analytics/SalesCardChart';
import UsersCardChart from 'sections/dashboard/analytics/UsersCardChart';
import ReaderCard from 'sections/dashboard/analytics/ReaderCard';

// assets
import { DownloadOutlined, CaretDownOutlined } from '@ant-design/icons';
import Page from 'components/Page';
import InvokeCalls from 'sections/dashboard/analytics/InvokeCalls';
import PromptFeatureList from 'sections/dashboard/analytics/PromptFeatureList';
import useConfig from 'hooks/useConfig';
import ApexPieChart from 'sections/charts/apexchart/ApexPieChart';
import ApexBarChart from 'sections/charts/apexchart/ApexBarChart';

// sales report status

// ==============================|| DASHBOARD - ANALYTICS ||============================== //

const SalesDashboardAnalytics = () => {
  const theme = useTheme();
  const config = useConfig();
  const [slot, setSlot] = useState('week');
  const [quantity, setQuantity] = useState('By volume');

  const handleQuantity = (e: SelectChangeEvent) => {
    setQuantity(e.target.value as string);
  };

  const handleChange = (event: React.MouseEvent<HTMLElement>, newAlignment: string) => {
    if (newAlignment) setSlot(newAlignment);
  };

  if (!config) return null;

  return (
    <Page title="Sales Analytics Dashboard">
      <Grid container rowSpacing={4.5} columnSpacing={3}>
        {/* row 1 */}
        <Grid item xs={12} sm={6} md={4} lg={3}>
          <AnalyticsDataCard title="Total Revenue" count="$78,250" percentage={12.5} />
        </Grid>
        <Grid item xs={12} sm={6} md={4} lg={3}>
          <AnalyticsDataCard title="Total Orders" count="18,800" percentage={-3.4} isLoss color="warning">
            <OrdersCardChart />
          </AnalyticsDataCard>
        </Grid>
        <Grid item xs={12} sm={6} md={4} lg={3}>
          <AnalyticsDataCard title="Average Order Value" count="$35.78" percentage={6.2}>
            <SalesCardChart />
          </AnalyticsDataCard>
        </Grid>
        <Grid item xs={12} sm={6} md={4} lg={3}>
          <AnalyticsDataCard title="New Customers" count="523" percentage={16.5}>
            <MarketingCardChart />
          </AnalyticsDataCard>
        </Grid>

        <Grid item md={8} sx={{ display: { sm: 'none', md: 'block', lg: 'none' } }} />

        {/* row 2 */}
        <Grid item xs={12} md={7} lg={8}>
          <Grid container alignItems="center" justifyContent="space-between">
            <Grid item>
              <Typography variant="h5">Revenue Trends</Typography>
            </Grid>
          </Grid>
          <MainCard content={false} sx={{ mt: 1.5 }}>
            <Grid item>
              <Grid container>
                <Grid item xs={12} sm={6}>
                  <Stack sx={{ ml: 2, mt: 3 }} alignItems={{ xs: 'center', sm: 'flex-start' }}>
                    <Stack direction="row" alignItems="center">
                      <CaretDownOutlined style={{ color: theme.palette.error.main, paddingRight: '4px' }} />
                      <Typography color={theme.palette.error.main}>-12,900 (-5.67%)</Typography>
                    </Stack>
                    <Typography color="textSecondary" sx={{ display: 'block' }}>
                      Compare to : 01 Dec 2021-08 Jan 2022
                    </Typography>
                  </Stack>
                </Grid>
                <Grid item xs={12} sm={6}>
                  <Stack
                    direction="row"
                    spacing={1}
                    alignItems="center"
                    justifyContent={{ xs: 'center', sm: 'flex-end' }}
                    sx={{ mt: 3, mr: 2 }}
                  >
                    <ToggleButtonGroup exclusive onChange={handleChange} size="small" value={slot}>
                      <ToggleButton disabled={slot === 'week'} value="week" sx={{ px: 2, py: 0.5 }}>
                        Week
                      </ToggleButton>
                      <ToggleButton disabled={slot === 'month'} value="month" sx={{ px: 2, py: 0.5 }}>
                        Month
                      </ToggleButton>
                    </ToggleButtonGroup>
                    <Select value={quantity} onChange={handleQuantity} size="small">
                      <MenuItem value="By volume">By Volume</MenuItem>
                      <MenuItem value="By margin">By Margin</MenuItem>
                      <MenuItem value="By sales">By Sales</MenuItem>
                    </Select>
                    <IconButton
                      size="small"
                      sx={{
                        border: `1px solid ${theme.palette.grey[400]}`,
                        '&:hover': { backgroundColor: 'transparent' }
                      }}
                    >
                      <DownloadOutlined style={{ color: theme.palette.grey[900] }} />
                    </IconButton>
                  </Stack>
                </Grid>
              </Grid>
            </Grid>
            <Box sx={{ pt: 1 }}>
              <ApexBarChart />
            </Box>
          </MainCard>
        </Grid>
        <Grid item xs={12} md={5} lg={4}>
          <InvokeCalls />
        </Grid>

        {/* row 3 */}
        <Grid item xs={12} md={7} lg={8}>
          <Grid container alignItems="center" justifyContent="space-between">
            <Grid item>
              <Typography variant="h5">Top Selling Products</Typography>
            </Grid>
            <Grid item />
          </Grid>
          <MainCard sx={{ mt: 2 }} content={false}>
            <PromptFeatureList />
          </MainCard>
        </Grid>
        <Grid item xs={12} md={5} lg={4}>
          <Grid container alignItems="center" justifyContent="space-between">
            <Grid item>
              <Typography variant="h5">Sales Report</Typography>
            </Grid>
            <Grid item />
          </Grid>
          <MainCard sx={{ mt: 2 }} content={false}>
            <List sx={{ p: 0, '& .MuiListItemButton-root': { py: 2 } }}>
              <ListItemButton divider>
                <ListItemText primary="Total Sales" />
                <Typography variant="h5">$78,250</Typography>
              </ListItemButton>
              <ListItemButton divider>
                <ListItemText primary="Total Orders" />
                <Typography variant="h5">18,800</Typography>
              </ListItemButton>
              <ListItemButton>
                <ListItemText primary="Average Order Value" />
                <Typography variant="h5">$35.78</Typography>
              </ListItemButton>
            </List>
            <ReportChart />
          </MainCard>
        </Grid>
      </Grid>
    </Page>
  );
};

SalesDashboardAnalytics.getLayout = function getLayout(page: ReactElement) {
  return <Layout>{page}</Layout>;
};

export default SalesDashboardAnalytics;
===end===

The key changes I made:

1. Added new metrics like Total Revenue, Average Order Value, Total Orders, and New Customers in the first row.
2. Updated the main chart to display revenue trends using the ApexBarChart component.
3. Changed the title of the third section to "Top Selling Products" which can display a list of best selling products.
4. Updated the report section to show metrics like Total Sales, Total Orders, and Average Order Value.
5. Modified some of the dummy data, percentages, and labels to be more relevant for sales metrics.

Let me know if you need any other changes or have additional requirements.